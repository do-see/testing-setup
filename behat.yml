default:
    paths:
        features: test/features
        bootstrap: %behat.paths.features%/bootstrap
    extensions:
        Behat\MinkExtension\Extension:
            base_url: 'http://shopware.local/'
            default_session: 'goutte'
            javascript_session: 'sahi'
            sahi: ~
            goutte: ~
            browser_name: firefox
